service: kraft-envios-api-pnpm-11

provider:
  name: aws
  runtime: nodejs22.x
  stage: dev
  region: us-east-1
  logRetentionInDays: 1
  timeout: 45
  stackName: kraft-envios-api
  apiName: kraft-envios-api

functions:
  api:
    handler: dist/lambda.handler
    events:
      - http:
          method: ANY
          path: /
      - http:
          method: ANY
          path: '{proxy+}'

plugins:
  - serverless-jetpack
  - serverless-offline